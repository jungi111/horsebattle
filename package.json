{
  "name": "horsebattle",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "친구들과 각자 기기에서 같은 경주를 보며 배팅하는 온라인 경마 게임",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "seed:members": "node scripts/seed-members.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  }
}
